# Example only. Do NOT put real keys in this file.
# Copy to `.env` and fill in your real key locally, OR use Colab Secrets (recommended).

LLM_PROVIDER=gemini
GEMINI_API_KEY=your_fake_key_here
GOOGLE_API_KEY=your_fake_key_here

# Optional overrides (defaults shown):
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_EMBED_MODEL=gemini-embedding-001
